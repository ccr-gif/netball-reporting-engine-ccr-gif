# --- Package managers / logs ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# If you want to track the lockfile, remove the next line:
package-lock.json

# --- Expo / Metro / bundler caches ---
.expo/
.expo-shared/
.expo/web/
.expo-dev-client/
dist/
.bundle/
.haul/

# --- React Native / platforms (typical build artifacts) ---
android/.gradle/
android/.idea/
android/app/build/
ios/build/
ios/Pods/
*.xcworkspace
*.xcodeproj/project.xcworkspace/

# --- TypeScript incremental build info ---
*.tsbuildinfo

# --- OS / editor cruft ---
.DS_Store
Thumbs.db

# --- Env / secrets ---
.env*
!.env.example
